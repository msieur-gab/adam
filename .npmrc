# Make optional dependencies truly optional
# This prevents build failures when packages like sharp can't be installed
optional=true

# Continue on error for optional dependencies
force=false

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=false
